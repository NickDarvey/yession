<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Not a product project and deliberately not in Yession.slnx, for the same reason the
    other fixtures are not: half the scopes below are meant to be reported. `lint` analyzes
    it on its own and checks the verdicts against the `// YES005` markers in the source.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <Compile Include="Scopes.fs" />
  </ItemGroup>

</Project>
